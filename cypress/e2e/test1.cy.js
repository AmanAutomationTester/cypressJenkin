
describe('block for config', () => {
  
    it('should run', () => {

      assert.ok(false);
    })
  
    it('should also work', () => {
      assert.ok(true);
    })

    it('should run as failures', () => {
      assert.ok(true);
    })
  })