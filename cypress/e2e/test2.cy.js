
describe('block for config', () => {
  
    it('python', () => {

      assert.ok(true);
    })
  
    it('pyTest', () => {
      assert.ok(false);
    })

    it('maven', () => {
      assert.ok(false);
    })
  })