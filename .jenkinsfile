pipeline
{
    agent any

    stages
    {
        // stage('Build')
        // {
        //     steps
        //     {
        //         // bat "npm install"
        //     }
        // }
        // stage ('Test')
        // {
        //     steps
        //     {
        //         echo 'Executing spec file in Cypress'
        //         bat "npm test"
        //     }
        // }
        stage ('Read failures json')
        {
            steps
            {
                script {
                    // def helperFile = findFiles(glob: '**/helper.groovy')
                    def var2 = load "helper.groovy"                    

                    // def helperFile 1= findFiles(glob: 'helper.groovy')
                    // echo helperFile1;


                    // helpers = load(helperFile[0].getPath())
                    var2.parseJsonToCSV();
                    
                }
            }
        }
    }
}