pipeline
{
    agent any

    stages
    {
        stage('Build')
        {
            steps
            {
                // bat "npm install"
            }
        }
        // stage ('Test')
        // {
        //     steps
        //     {
        //         echo 'Executing spec file in Cypress'
        //         bat "npm test"
        //     }
        // }
        stage ('Read failures json')
        {
            steps
            {
                script {
                    def helperFile = findFiles(glob: '**/helpers.groovy')
                    helpers = load(helperFile[0].getPath())
                    helpers.parseJsonToCSV();
                }
            }
        }
    }
}