pipeline
{
    agent any

    stages
    {
        // stage('Build')
        // {
        //     steps
        //     {
        //         // bat "npm install"
        //     }
        // }
        // stage ('Test')
        // {
        //     steps
        //     {
        //         echo 'Executing spec file in Cypress'
        //         bat "npm test"
        //     }
        // }
        stage ('Read failures json')
        {
            steps
            {

                
                script {
                    def files = findFiles(glob: '**/logs/*.json')
                    files.each{name ->                   
                        def props = readJSON file: "${name}"
                        def records = [['key'], [props.title]]
                        // echo "megha ${props.title}"
                        def list = []
                        list.add(props.title);
                        echo "aman singhal ${list}"
                        writeCSV file: 'fail.csv', records: records, format: CSVFormat.EXCEL;
                        
                    }              
                    
                }
            }
        }
    }
}